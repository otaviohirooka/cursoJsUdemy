const amazonas = ['Marin', 'June']
const cavaleiros = ['Hyoga', 'Ikki']

const todos = amazonas.concat(cavaleiros)
console.log(amazonas, cavaleiros, todos)

console.log([].concat([1, 2], [3, 4], 5, [[6, 7]]))